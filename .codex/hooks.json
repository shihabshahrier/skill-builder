{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'skill-builder available. /skill-builder \"description\" [--type workflow|mode|tool|reference|agent] [--repo] [--audit path] [--fix]. Phases: route → classify → gather → research → SKILL.md → references → hooks → scaffold → report. See skills/skill-builder/SKILL.md.'",
            "timeout": 5,
            "statusMessage": "Loading skill-builder..."
          }
        ]
      }
    ]
  }
}
